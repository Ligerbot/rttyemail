# RTTY versions

This folder `rtty/` contains the old version of the project that uses RTTY. It will be really slow and may not work at all. Unless you have a specific purpose for using it, you should probably stay with the current FSK one.
