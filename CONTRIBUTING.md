# How to contribute to the code

First make your changes to the code. Then, when you are ready run `git add .` from the top level directory and then `git commit -m "Your message here"` then finally run git push.
